# Looping through Sets

# You can use a for loop to iterate  through the set items

thisset = {"apple", "banana", "cherry"}
for x in thisset:
    print(x)

# Can't use a while loop as items can't be referred to by their index
